import { LineHorizontal } from '~/theme';

/**
 * -----------------------------------------------------------------------------
 * Action Icon button that closes the Modal on tap.
 */
export function ListItemSeparator() {
  return <LineHorizontal marginVertical="none" maxWidth="100%" opacity={0.1} width="100%" />;
}
